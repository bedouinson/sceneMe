import { useState, useEffect, useNavigte } from "react";
import { AuthContext } from "../Context/AuthContext";

const API_KEY = process.env.REACT_APP_TMDB_API_KEY;

const Lists = () => {
  return (
    <div>
      <h1>Lists</h1>
    </div>
  );
};

export default Lists;
